<%= form_for(@bracket) do |f| %>
  <% if @bracket.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bracket.errors.count, "error") %> prohibited this bracket from being saved:</h2>

      <ul>
      <% @bracket.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :isStarted %><br>
    <%= f.check_box :isStarted %>
  </div>
  <div class="field">
    <%= f.label :reseed %><br>
    <%= f.check_box :reseed %>
  </div>
  <div class="field">
    <%= f.label :isFinished %><br>
    <%= f.check_box :isFinished %>
  </div>
  <div class="field">
    <%= f.label :tournament_id %><br>
    <%= f.text_field :tournament_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
